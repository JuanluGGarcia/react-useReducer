import Counter from "./components/counter/Counter"

/* Para crear un useReducer:
1.- Dentro de src tenemos que crear una carpeta llamada 'reducers'
2.- Lo llamamos al archivo como queramos ...Reducer.js */

const App = () => {
	return <Counter />;
};

export default App;
